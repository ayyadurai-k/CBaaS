# CBaaS AWS Configuration
# This file contains your AWS resource identifiers for easy reference
# DO NOT COMMIT THIS FILE if it contains sensitive information

# AWS Account
AWS_ACCOUNT_ID=577897067437
AWS_REGION=ap-south-1

# S3 Bucket
S3_BUCKET=cbaas-vite-app
S3_WEBSITE_ENDPOINT=cbaas-vite-app.s3-website.ap-south-1.amazonaws.com

# CloudFront Distribution
CF_DIST_ID=E3ACPM7RLVZA5I
CF_ARN=arn:aws:cloudfront::577897067437:distribution/E3ACPM7RLVZA5I

# IAM Role for GitHub Actions
IAM_ROLE_NAME=GitHubActionsDeployRole
IAM_ROLE_ARN=arn:aws:iam::577897067437:role/GitHubActionsDeployRole

# GitHub Secrets (configure these in GitHub repository settings)
# Settings → Secrets and variables → Actions → New repository secret
#
# Secret Name: AWS_ROLE_ARN
# Value: arn:aws:iam::577897067437:role/GitHubActionsDeployRole
#
# Secret Name: S3_BUCKET
# Value: cbaas-vite-app
#
# Secret Name: CF_DIST_ID
# Value: E3ACPM7RLVZA5I

# Quick Deployment Commands
# -------------------------

# Manual deployment (from frontend directory):
# ../infra/aws/deploy_frontend.sh cbaas-vite-app E3ACPM7RLVZA5I

# Trigger GitHub Actions deployment:
# git push origin release

# View CloudFront distribution:
# aws cloudfront get-distribution --id E3ACPM7RLVZA5I

# Check deployment status:
# aws cloudfront list-invalidations --distribution-id E3ACPM7RLVZA5I
